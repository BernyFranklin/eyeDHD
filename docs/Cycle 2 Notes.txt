Test Cases


Feature: Animation Generation
	Action
	Expected Result
	Test Case 1
	Click Play/Resume Animation Button
	Animation Pauses on Frame; Animation Resumes on the same Frame
	Test Case 2
	New Data from csv file is loaded
	The animation begins using new data, and recording starts automatically from the first frame of the new dataset. The video is then saved as a .webm.?
	Test Case 3
	User Selects CSV file that is missing required columns (e.g. LeftEyeForwardX, or any required header)
	Prevent animation from starting. An error message is displayed stating that the CSV is invalid.
	



Feature: Read and Write Data to Database
	Action
	Expected Result
	Test Case 1
	Reads data from the CSV Data Samples
	Cleans the sample dataset
	Test Case 2
	Reads back data from SQLite for Animation 
	App connects to the SQLite database file (correct file path)
	Test Case 3
	Database Error Handling 
	No partial data is written
If DB fails, animation doesn’t start
	

















Feature: UI Things and Stuff
	Action
	Expected Result
	Test Case 1
	User clicks Play, then Pause, then Play again during the animation playback
	Animation pauses on the current frame and resumes from the same frame, without skipping frames or restarting
	Test Case 2
	Load a new valid CSV file after a previous dataset was already animated
	Animation resets to frame 0 of the new dataset, begins playback using new data
	Test Case 3
	Allow animation to run with recording enabled until completion 
	A .webm video file is generated successfully and saved with a unique name