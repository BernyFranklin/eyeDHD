- Work on eye dilation chart
- Animation generation progress bar
- UI Rework / UX Simplification
- CSS Styling
- Change so the user selects a projects directory which the app stores the database, user settings, and a folder that contains the folders for each case / trial. Each case / trial folder would contain: backups of user imported file like the csv and vr video incase it is needed again; all the generated outputs like animations, side-by-sides, images of graphs, etc which will be copied out when the user exports them