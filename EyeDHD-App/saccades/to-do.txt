TO-DO
X = DONE

X Make a directory for saccades

Create and implement these files:
Up to date structure 
saccades/
  index.ts (API) 
  detection.ts (Threshold and grouping logic) (Done)
  schema.ts                                   (Done)
  velocities.ts (Angular Velocity Math)       (Done)
  metrics/
    index.ts
    metrics.ts
    types.ts
    stats.ts
    filtering.ts
  test/
    metrics.test.ts                           (Done)
    detection.test.ts                         (Done)
    velocity.test.ts                          (Done)  
  

X Add a README for reference

//-----------------------------------------------------------
Phase 3: metrics.ts — tests-first spec
Assumed inputs

saccades: SaccadeEvent[] (already detected; metrics never re-detects)

options?: MetricOptions

segments?: Segment[] (optional; each has { label, startTsMs, endTsMs })

Required fields per saccade for metrics

startTsMs, endTsMs

amplitudeDeg

peakVelocityDegPerSec

(any other fields are allowed; metrics ignores what it doesn’t need)

/saccades/
  index.ts (API) 
  detection.ts
  schema.ts                          
  velocities.ts
  /metrics/
    index.ts
    metrics.ts
    types.ts
    stats.ts
    filtering.ts
  /test/
    metrics.test.ts                       
    detection.test.ts                      
    velocity.test.ts             
  